# Optional secondary launcher definition for jpackage.
# Can be enabled later with:
# --add-launcher HoursDesktop=src/packaging/jpackage/app-launcher.properties
arguments=--spring.profiles.active=desktop
java-options=-Dfile.encoding=UTF-8
