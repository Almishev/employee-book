<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.6</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.trackng</groupId>
	<artifactId>hours</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name/>
	<description/>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>17</java.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>

		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.12.6</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.12.6</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.12.6</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>desktop-windows</id>
			<activation>
				<os>
					<family>windows</family>
				</os>
			</activation>
			<properties>
				<!-- MSI ProductVersion: x.y.z (без SNAPSHOT); override: -Ddesktop.app.version=1.2.3 -->
				<desktop.app.version>0.0.6</desktop.app.version>
				<desktop.app.name>Hours</desktop.app.name>
				<desktop.app.vendor>TrackNG</desktop.app.vendor>
				<!-- Без това прякътът отива под Start Menu → Programs → „Unknown“ -->
				<desktop.menu.group>${desktop.app.vendor}</desktop.menu.group>
				<desktop.installer.type>msi</desktop.installer.type>
				<desktop.runtime.dir>${project.build.directory}/jpackage/runtime</desktop.runtime.dir>
				<desktop.input.dir>${project.build.directory}/jpackage/input</desktop.input.dir>
				<desktop.dest.dir>${project.build.directory}/installer</desktop.dest.dir>
				<desktop.resource.dir>${project.basedir}/src/packaging/jpackage</desktop.resource.dir>
			</properties>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-antrun-plugin</artifactId>
						<version>3.1.0</version>
						<executions>
							<execution>
								<id>prepare-jpackage-input</id>
								<phase>package</phase>
								<goals>
									<goal>run</goal>
								</goals>
								<configuration>
									<target>
										<delete dir="${desktop.runtime.dir}" quiet="true" />
										<mkdir dir="${desktop.input.dir}" />
										<copy file="${project.build.directory}/${project.build.finalName}.jar"
											tofile="${desktop.input.dir}/${project.build.finalName}.jar" overwrite="true" />
									</target>
								</configuration>
							</execution>
						</executions>
					</plugin>

					<plugin>
						<groupId>org.codehaus.mojo</groupId>
						<artifactId>exec-maven-plugin</artifactId>
						<version>3.5.0</version>
						<executions>
							<execution>
								<id>create-runtime-image-from-jmods</id>
								<phase>package</phase>
								<goals>
									<goal>exec</goal>
								</goals>
								<configuration>
									<executable>jlink</executable>
									<arguments>
										<!-- Пълен JDK runtime: всички jmods от текущия JDK (изисква JDK с папка jmods, не само JRE) -->
										<argument>--module-path</argument>
										<argument>${java.home}/jmods</argument>
										<argument>--add-modules</argument>
										<argument>ALL-MODULE-PATH</argument>
										<argument>--strip-debug</argument>
										<argument>--no-header-files</argument>
										<argument>--no-man-pages</argument>
										<argument>--output</argument>
										<argument>${desktop.runtime.dir}</argument>
									</arguments>
								</configuration>
							</execution>
							<execution>
								<id>build-windows-installer</id>
								<phase>package</phase>
								<goals>
									<goal>exec</goal>
								</goals>
								<configuration>
									<executable>jpackage</executable>
									<arguments>
										<argument>--type</argument>
										<argument>${desktop.installer.type}</argument>
										<argument>--name</argument>
										<argument>${desktop.app.name}</argument>
										<argument>--vendor</argument>
										<argument>${desktop.app.vendor}</argument>
										<argument>--app-version</argument>
										<argument>${desktop.app.version}</argument>
										<argument>--dest</argument>
										<argument>${desktop.dest.dir}</argument>
										<argument>--input</argument>
										<argument>${desktop.input.dir}</argument>
										<argument>--main-jar</argument>
										<argument>${project.build.finalName}.jar</argument>
										<!-- Без изричен main-class: jpackage ползва Main-Class от MANIFEST.MF (-jar). Изричен JarLauncher понякога води до -cp и „Failed to launch JVM“. -->
										<argument>--runtime-image</argument>
										<argument>${desktop.runtime.dir}</argument>
										<argument>--resource-dir</argument>
										<argument>${desktop.resource.dir}</argument>
										<argument>--icon</argument>
										<argument>${desktop.resource.dir}/hours.ico</argument>
										<argument>--java-options</argument>
										<argument>-Dfile.encoding=UTF-8</argument>
										<argument>--java-options</argument>
										<argument>-Dspring.profiles.active=desktop</argument>
										<argument>--win-dir-chooser</argument>
										<argument>--win-menu</argument>
										<argument>--win-menu-group</argument>
										<argument>${desktop.menu.group}</argument>
										<argument>--win-shortcut</argument>
									</arguments>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
